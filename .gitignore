# Docker volumes (datos locales, no van al repo)
postgres-data/
redis-data/

# Archivos subidos por usuarios
uploads/

# Variables de entorno (contienen secretos)
.env
.env.local
.env.*.local
.env.vaultwarden
.env.backup
stp-api/.env

# Node
stp-api/node_modules/
stp-api/dist/
stp-landing/node_modules/
stp-landing/.next/
stp-landing/dist/

# Mobile — repo independiente
stp-mobile/

# AdGuard — datos de runtime + config viva (contiene hash de password de admin)
adguard/work/
adguard/conf/AdGuardHome.yaml

# Logs
*.log
npm-debug.log*

# Estado runtime del bot de aprobación de cuentas Nextcloud (no versionar)
scripts/.nextcloud-gate-pending
scripts/.nextcloud-gate-notified
scripts/.telegram-poller-offset
scripts/.telegram-poller.lock

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# TypeScript build
*.tsbuildinfo
immich/.env
caddy/downloads/

# Config local de Claude Code (permisos/ajustes por usuario, no del proyecto)
.claude/settings.local.json
